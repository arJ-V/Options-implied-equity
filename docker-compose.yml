services:
  dashboard:
    build: .
    ports:
      - "8501:8501"
    volumes:
      - ./data/processed:/app/data/processed:ro
      - ./data/raw/release:/app/data/raw/release:ro
    environment:
      - STREAMLIT_BROWSER_GATHER_USAGE_STATS=false
