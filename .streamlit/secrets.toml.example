# Copy to .streamlit/secrets.toml for local overrides (do not commit secrets.toml).

[data]
# Optional override. Default: data/processed/signals.parquet if present, else data/sample/signals.parquet
signals_path = "data/sample/signals.parquet"
